html
    head
    body
        form(action='/login', method='post')
            input(type='hidden', name='_csrf', value=csrfToken)
            label="Username"
            input(type='name', name='username', required=true)
            label="Password"
            input(type='password' name='password' required=true)
            input(type='submit', value="Submit")